syntax = "proto3";

message Nested {
    int32 value = 1;
}

message Foo {
    int32 test = 1;
    Nested nested = 2;
}

service Bar{
    rpc Baz(Foo) returns (Foo);
    rpc BazClientStream(stream Foo) returns (Foo);
    rpc BazServerStream(Foo) returns (stream Foo);
    rpc BazBidiStream(stream Foo) returns (stream Foo);
}
