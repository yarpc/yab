service: foo
headers:
  header1: ${user:foo}
request:
  emptyfallback: ${unspecified:}
  fallback: ${unspecified:fallback}
  fallbacklist: ${unspecified:[1,2]}
  noReplace: \${user} \\\${
  replaceList: ${uuids}
  replaced: ${user:u}
